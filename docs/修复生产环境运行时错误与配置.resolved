# 修复生产环境运行时错误与配置

## 问题描述
1.  **API 连通性故障**：[httpClient.ts](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/api/src/httpClient.ts) 硬编码了 `localhost:3000`，导致公网访问时前端无法正常与后端通讯。
2.  **前端组件崩溃**：[formatPrice](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/utils/src/format.ts#31-37) 工具函数未处理 `undefined` 或 `null` 值，在渲染列表或详情时若数据缺失会导致 JS 引擎报错并崩溃。
3.  **环境变量配置失效**：Dockerfile 中传入的 `VITE_API_URL` 未在代码中被正确消费。

## 方案设计

### 1. 核心工具库修复 (Packages)

#### [MODIFY] [httpClient.ts](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/api/src/httpClient.ts)
- 引入 `import.meta.env.VITE_API_URL` 作为 `baseURL` 的首选来源。
- 设置生产环境默认值为 `/api`（对应 Nginx 配置），开发环境回退到本地地址。

#### [MODIFY] [format.ts](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/utils/src/format.ts)
- 为 [formatPrice](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/utils/src/format.ts#31-37) 增加 `typeof value !== 'number'` 检查，若非数字则返回默认占位符。
- 为 [formatHeat](file:///Users/liuliu/Projects/advertise-wall/monorepo/packages/utils/src/format.ts#38-49) 增强容错处理。

### 2. 环境变量注入校验

- 确认 Dockerfile 中 `ARG VITE_API_URL` 在构建阶段被正确传递（目前已有配置，无需改动，只需代码侧生效）。

## 验证计划

### 自动化验证计划
- 在本地运行 `pnpm build` 命令，观察编译过程是否因 `import.meta.env` 类型问题报错（已修复 tsconfig，应通畅）。

### 手动验证步骤
1. 修改代码并推送到 GitHub。
2. 在服务器上执行 `git pull` 和 `docker compose up -d --build`。
3. **验证点 A**：查看浏览器控制台，确认 `XHR` 请求的 URL 指向 `http://121.40.134.147/api/xxx` 而非 `localhost`。
4. **验证点 B**：登录成功后跳转到首页，确认页面能渲染（即便某些广告数据不全，也不会触发 JS Error）。
